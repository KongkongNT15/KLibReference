<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PageEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PageEditor"
    xmlns:pages="using:WebPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PageEditor">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt" />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions >
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <MenuBar Grid.Row="0" >
            <MenuBarItem Title="ページ">
                <MenuFlyoutItem Text="新規作成" Click="MenuBar_MenuFlyoutItem_Click" >
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="N" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Open..."/>
                <MenuFlyoutItem Text="Save"/>
                <MenuFlyoutItem Text="Exit"/>
            </MenuBarItem>

            <MenuBarItem Title="編集">
                <MenuFlyoutItem Text="Undo"/>
                <MenuFlyoutItem Text="Cut"/>
                <MenuFlyoutItem Text="Copy"/>
                <MenuFlyoutItem Text="Paste"/>
            </MenuBarItem>

            <MenuBarItem Title="ヘルプ">
                <MenuFlyoutItem Text="About"/>
            </MenuBarItem>

        </MenuBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TreeView
                Grid.Column="0"
                x:Name="FolderTree"
                Width="300"
                SelectionChanged="FolderTree_SelectionChanged">
                <TreeView.ItemTemplate >
                    <DataTemplate x:DataType="pages:PageItem">
                        <TreeViewItem 
                            ItemsSource="{x:Bind Children}">

                            <TreeViewItem.ContextFlyout>
                                <MenuFlyout >
                                    <MenuFlyoutItem Text="追加" Click="FolderTree_MenuFlyoutItem_Add_Click" Tag="{x:Bind This}" />
                                    <MenuFlyoutItem Text="削除" Icon="Delete" Tag="{x:Bind This}" />
                                </MenuFlyout>
                            </TreeViewItem.ContextFlyout>
                            
                            <StackPanel Spacing="20" Orientation="Horizontal">
                                <FontIcon Glyph="{x:Bind Glyph}" />
                                <TextBlock Text="{x:Bind Name}" />

                            </StackPanel>
                        </TreeViewItem>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions >
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <TabView
                    Grid.Row="0"
                    x:Name="Tabs"
                    AddTabButtonClick="Tabs_AddTabButtonClick"
                    SelectionChanged="Tabs_SelectionChanged"
                    >

                    <TabView.TabItemTemplate>
                        <DataTemplate x:DataType="pages:PageItem" >
                            <TabViewItem 
                                Header="{x:Bind Name}">

                                <TabViewItem.IconSource>
                                    <FontIconSource Glyph="{x:Bind Glyph}" />
                                </TabViewItem.IconSource>
                            </TabViewItem>
                        </DataTemplate>
                    </TabView.TabItemTemplate>
                </TabView>

                <ScrollViewer Grid.Row="1" >
                    <StackPanel >
                        <UserControl x:Name="Contents" />
                        <Rectangle Height="24" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        

        
    </Grid>

    
</Window>
