<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PageEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PageEditor"
    xmlns:pages="using:PageEditor.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PageEditor">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TreeView Grid.Column="0" x:Name="FolderTree" Width="400" >
            <TreeView.ItemTemplate >
                <DataTemplate x:DataType="pages:PageItem">
                    <pages:PageTreeItem Title="{x:Bind Name}" Type="{x:Bind Type}" ItemsSource="{x:Bind Children}" />
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <ScrollViewer Grid.Column="1" >
            <StackPanel >
                <UserControl x:Name="Contents" />
                <Rectangle Height="24" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
